__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
/render-test-out/
*.egg-info/

# Generated docs-site output + build cache (docs/publishing)
/build/
/.cache/

# Optional Obsidian vault state, IF someone opens the plain-Markdown PCDA/ reference
# docs as a vault (not required to read them; also excluded from renders via copier.yml
# _exclude). Editor-local; not part of the template.
.obsidian/

# Personal Claude Code permissions overlay; the shared settings.json is tracked.
.claude/settings.local.json

# Local tool/editor state — not part of the template.
.claude/worktrees/
.claudian/
