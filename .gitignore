__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
/render-test-out/
*.egg-info/

# Generated docs-site output + build cache (docs/publishing)
/build/
/.cache/

# Obsidian vault state for the PCDA/ reference docs (also excluded from renders
# via copier.yml _exclude). Editor-local; not part of the template.
.obsidian/

# Personal Claude Code permissions overlay; the shared settings.json is tracked.
.claude/settings.local.json

# Local tool/editor state — not part of the template.
.claude/worktrees/
.claudian/
